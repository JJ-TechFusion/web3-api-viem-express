# Web3 API Viem Express

Minimal Express API with viem dependency to trigger web3 detection.

Expected StackShift behavior:

- runtime: `nodejs`
- framework: `express`
- service type: `web`
- workload type: `web`
- web3 enabled: `true`
